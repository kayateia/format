//
// Copyright (c) 2021 Kayateia
// Please see README.md in the root of this project for licence info.
//

// This bridges from the SDK callbacks to updating our "clean" state.

/* import { State } from "../model";

export function syncComplete(stateCode: string): Updater {
    return (state: State): State => {
        console.log("sync:", stateCode);
        return state;
    };
};

export function roomMessage(eventContent: any): Updater {
    return (state: State): State => {
        console.log("received event:", eventContent.body);
        return state;
    }
};
*/
